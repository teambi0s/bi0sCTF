<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Validate URL - SFS</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body.neon-body {
      background: linear-gradient(135deg, #0f0c29, #24243e, #302b63);
      color: white;
      font-family: 'Arial', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      width: 100%;
      max-width: 500px;
      text-align: center;
      padding: 20px;
    }

    h1 {
      font-size: 2.5rem;
      font-weight: bold;
      background: linear-gradient(45deg, #8a2be2, #4b0082, #0000ff);
      -webkit-background-clip: text;
      text-shadow: 0 0 5px #8a2be2, 0 0 10px #4b0082, 0 0 15px #0000ff;
      margin-bottom: 20px;
    }

    .flash-error {
      color: red;
      font-size: 1rem;
      margin-bottom: 10px;
    }

    .flash-notice {
      color: green;
      font-size: 1rem;
      margin-bottom: 10px;
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 10px;
      color: #dcdcdc;
    }

    .neon-highlight {
      color: #8a2be2;
    }
  </style>
</head>
<body class="neon-body">
  <div class="container">
    <h1>Validate URL</h1>

    <% if flash[:notice] %>
      <p class="flash-notice"><%= flash[:notice] %></p>
    <% end %>

    <% if flash[:error] %>
      <p class="flash-error"><%= flash[:error] %></p>
    <% end %>

    <p>User: <span class="neon-highlight"><%= @user.username %></span></p>
    <p>URL: <span class="neon-highlight"><%= @user.url %></span></p>
    <p>Validated: <span class="neon-highlight"><%= @user.validated ? "Yes" : "No" %></span></p>
  </div>
</body>
</html>